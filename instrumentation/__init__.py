# Marks instrumentation/ as a package so `python -m instrumentation.episode_runner`
# and `from instrumentation.nvml_logger import NVMLLogger` both work regardless
# of the caller's working directory.
